#Remember that when creating a git me commit you use -m and then add a message lets call it git message " git commit -m 'My first commit'"
Use git branch to see and create new branches and use git checkout to switch between branches and then use git merge to merge branches and use git status to check the level of your activity.
